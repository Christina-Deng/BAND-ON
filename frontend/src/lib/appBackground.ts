export type AppBackgroundVariant = 'ink' | 'print';

/** Logged-in page background. `print` = halftone gig poster; `ink` = brush / splashed ink. */
export const APP_BACKGROUND: AppBackgroundVariant = 'print';
